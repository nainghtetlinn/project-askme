import { Container } from "@mui/material";
import { useQuery } from "react-query";

export const Home = () => {
  return (
    <>
      <Container maxWidth="lg" sx={{ pt: 1 }}>
        Home
      </Container>
    </>
  );
};
